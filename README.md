# Experiment 8: File Upload (Node.js + Multer + MySQL)

## Overview
Simple server-side file upload: files saved to `uploads/` and filename recorded in MySQL.

## Requirements covered
- HTML form with enctype="multipart/form-data"
- multer for upload handling
- file path/name stored in DB
- file type & size validation

## Setup
1. Install dependencies:

